;; ;;; -*- lexical-binding: t -*-

(use-package color-theme-modern
  :disabled ;; Lexical binding warnings.
  ;; https://github.com/emacs-jp/replace-colorthemes?tab=readme-ov-file
  :defer t)

(use-package color-theme-sanityinc-tomorrow
  :disabled ;; Cyclic gnus-group-news-6 foreground face.
  :defer t)

(use-package creamsody-theme
  :defer t)

(use-package darktooth-theme
  :defer t)

(use-package doric-themes
  :defer t)

(use-package ef-themes
  :defer t)

(use-package gruber-darker-theme
  :disabled ;; rarely used
  :defer t)

(use-package gruvbox-theme
  :disabled ;; gnus-group-news-low
  :defer t)

(use-package kuronami-theme
  :disabled
  :defer t)

(use-package modus-themes
  :defer t)

(use-package nano-theme
  :disabled ;; rarely used
  :defer t)

(use-package solarized-theme
  :defer t)

(use-package standard-themes :defer t)

(use-package timu-rouge-theme
  :disabled ;; rarely used
  :defer t)

(use-package zenburn-theme :defer t)
