# -*- mode: snippet -*-
# name: Commit from ticketed branch
# key: ,com
# condition: (and (project-current) (magit-get-current-branch))
# --
`
(let* ((branch (magit-get-current-branch))
       (match-found (string-match "\\([A-Z]+\\)-\\([0-9]+\\)" branch)))
  (if match-found
      (format "%s-%s: " (upcase (match-string 1 branch)) (upcase (match-string 2 branch)))
    ""))
`$0