#+STARTUP: show2levels content indent

* Install
** Dependencies
*** [[https://brew.sh/][Brew]]
#+begin_src bash
brew update
brew install -v --quiet \
     asdf \
     autoconf \
     btop \
     direnv \
     fd  \
     fish \
     fzf  \
     gcc \
     git  \
     gnupg  \
     jq \
     libgccjit \
     lazygit \
     ripgrep \
     tlrc \
     tree-sitter \
     zoxide
#+end_src

#+begin_src bash :async :session *install* :results silent
brew install --cask \
     font-jetbrains-mono-nerd-font \
     ghostty
#+end_src

#+begin_src bash
brew tap d12frosted/emacs-plus
brew install emacs-plus
#+end_src

*** Rust / Cargo Dependencies
#+begin_src bash
curl https://sh.rustup.rs -sSf | sh
#+end_src

#+begin_src bash
cargo install emacs-lsp-booster
#+end_src

*** ASDF
**** Nodejs / NPM
#+begin_src bash :async :session *install* :results silent
asdf plugin add nodejs https://github.com/asdf-vm/asdf-nodejs.git
asdf install nodejs latest
asdf set -u nodejs latest
#+end_src
** Configs
*** Emacs
#+begin_src bash
ln -s $(pwd)/config/emacs ~/.config/emacs
cp $(pwd)/config/emacs/docs/example.custom.el ~/.config/emacs/custom.el
#+end_src

*** Fish
#+begin_src bash
ln -s $(pwd)/config/defaults.fish ~/.config/fish/conf.d/defaults.fish
#+end_src

Set default shell to fish.
#+begin_src text :results silent
command -v fish | sudo tee -a /etc/shells
chsh -s "$(command -v fish)"
#+end_src
*** Git
#+begin_src bash
mkdir -p ~/.config/git
cp $(pwd)/config/gitconfig ~/.config/git/config
cp $(pwd)/config/gitignore ~/.config/git/ignore
#+end_src

*** Ghostty
#+begin_src bash
mkdir -p ~/.config/ghostty
cp $(pwd)/config/ghostty.config ~/.config/ghostty/config
#+end_src

*** Sqlite
#+begin_src bash
cp $(pwd)/config/sqliterc ~/.sqliterc
#+end_src

*** Tmux
#+begin_src bash
mkdir -p ~/.config/tmux
cp $(pwd)/config/tmux.config ~/.config/tmux/config
#+end_src
